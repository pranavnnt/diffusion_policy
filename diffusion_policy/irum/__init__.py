"""IRUM: a frozen slow chunk policy, a bounded fast corrector, and an upward
message from the fast level to the slow one.

Ported from the ``cap``/``drawer`` benchmarks in the reactive-policy repo
(``~/dap``) so it can run inside this repository's dressing stack.  See
``diffusion_policy/irum/README.md`` for what changed in the port and why.
"""
